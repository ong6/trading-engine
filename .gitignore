# local state — never leaves the box
store/
logs/
scratch/
*.lock
!uv.lock
.env
.env.*
*.pem
*.key

# python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
build/
dist/
*.egg-info/

# node (ui/)
ui/node_modules/
ui/.next/

# raw source-derived exports, regenerated by every nightly (data/screens/ — the
# screen REPORTS — stay tracked; they are the synced deliverable)
data/eod/
data/universe.csv
