# Lessons — `adaptive_mr`

**Append-only.** Every entry is dated and stays; nothing here is ever edited or
deleted, because the value of this file is that it records what was believed at
the time, including the beliefs that turned out to be wrong. New entries go at
the BOTTOM.

Written by the book's own agent sessions (via `agents/validator.py`, which
appends whatever `lesson` field a session returns). A session reads this whole
file before proposing anything.

---

## 2026-08-04 — registration

No evidence yet. The book starts at its twin's exact parameters and the agent's
first real session is the one that matters. Standing instruction until there is
a record: **propose no change unless the evidence names a specific, dated,
measured effect.** "Markets look choppy" is not evidence; "of 11 closed losers,
8 had an MAE beyond −5% within 2 sessions of entry" is.
