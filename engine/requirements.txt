duckdb>=1.5
numpy>=2.0
pandas>=3.0
PyYAML>=6.0
requests>=2.31
websockets>=17.1
yfinance>=1.5
pandas_market_calendars>=5.4
fastapi>=0.139
uvicorn[standard]>=0.53.0
# engine/signals.py: this network filters on TLS fingerprint (plain requests dies
# mid-stream on FRED), so the macro collector uses curl_cffi's chrome impersonation.
curl_cffi>=0.15
openpyxl>=3.1
# AAII publishes the sentiment survey as a legacy BIFF .xls, which only xlrd reads.
xlrd>=2.0
